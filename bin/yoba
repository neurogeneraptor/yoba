#!/usr/bin/env bash

LIB="/usr/local/lib/yoba"

case "$1" in
  -h|--hui)
    "$LIB/yoba-h.sh"
    ;;
  -p|--pizda)
    "$LIB/yoba-p.sh"
    ;;
  -a|--ansible)
    "$LIB/yoba-ansible.sh"
    ;;
  *)
    echo "🧠 usage: yoba [-h|--hui] [-p|--pizda] [-a|--ansible]"
    exit 1
    ;;
esac
